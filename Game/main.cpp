#include "GameGL.h"

int main(int argc, char* argv[])
{
	GameGL * game = new GameGL(argc, argv); // creates an instance of your game
	return 0; // assumes a succesfull exit if your game exits and we make it to this
}